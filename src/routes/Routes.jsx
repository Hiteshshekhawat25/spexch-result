import { Routes } from "react-router-dom";


const RoutesComp = ({ socket }) => {
    return (
      <>
        <Routes fallbackElement={<Spinner color="red" />}>
        
        </Routes>
      </>
    );
  };